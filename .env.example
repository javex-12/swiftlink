NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only. Bypasses RLS entirely — never prefix this with NEXT_PUBLIC_ and
# never import anything that reads it into a client component.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key